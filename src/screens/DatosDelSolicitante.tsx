import React from "react";
import { Text } from "react-native";

const DatosDelSolicitante = ({ stepper, onSubmit, data }) => {
  return <Text>DatosDelSolicitante</Text>;
};

export default DatosDelSolicitante;
